-- generated_rule Emacs
if get_application_name() == "emacs" then
   -- undecorate_window()
   -- maximize()
   set_window_workspace(1)
   -- maximize_vertically()
   -- maximize_horizontally()
   debug_print("match")
end
